plugins {
  id 'org.ajoberstar.defaults' version '0.9.1'
  id 'java'
}

group = 'org.ajoberstar'

sourceCompatibility = '1.8'

repositories {
  mavenCentral()
}

dependencies {
  compile 'org.slf4j:slf4j-api:+'
  runtime 'ch.qos.logback:logback-core:+'
}

model {
  bintray {
    owner = 'ajoberstar'
    repo = 'test'
    pkg = 'pipeline-test'
  }
}
